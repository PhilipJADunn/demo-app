version : 0.2

env:
  variables:
    TEMPLATE_FILE: 'template.yaml'

phases:
  install:
    runtime-versions:
      nodejs: 12

  pre_build:
    commands:
      - pip install awscli==1.19.*
      - aws --version

  build:
    commands:
      - sam build
      - ls -l
      - sam package --template-file .aws-sam/build/template.yaml --output-template-file packaged-template.yaml --s3-bucket my-test-app-phil-bucket
                    --image-repository 658251713809.dkr.ecr.eu-west-2.amazonaws.com/demo-app

artifacts:
  type: zip
  files:
    - template.yaml
    - template_imageprocessor_eu-west-1.yml
